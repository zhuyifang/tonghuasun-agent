# 0.2.12 更新说明

这次更新提高了账户资产展示的可靠性，并明确区分 MCP 可直接使用的工具和只能通过本机 API 调用的交易接口。

## 本次更新

- 账户资产与持仓优先使用同花顺远航版返回的结构化数据，不再从界面文字猜测资金字段。
- 当总资产与持仓市值明显矛盾时，不再展示推算金额，避免错误的资产和仓位占比。
- 建委托单、撤单、改单、下发下单命令、批量下单和交易命令透传会出现在 MCP 能力列表中，并明确标注“仅支持 API”。
- 上述六项能力通过 MCP 调用时只返回对应 API 地址，不会直接执行交易。
- 本机接口文档统一使用“同花顺远航版”等面向用户的名称，并在交易接口中注明调用方式。
- Codex、Claude Code、WorkBuddy、ZCode、OpenClaw 和 DeepSeek Harness 的安装包统一升级到 0.2.12。

## 安装

你可以直接让正在使用的 AI 助手安装本项目；自动安装失败时，再从本页附件下载对应安装包手动安装。升级完成后请重启同花顺；如果没有显示新工具，请优先新建任务或重新加载插件。

## 兼容说明

- 已在同花顺远航版 11.4.1.3 上完成账户资产、持仓、REST API 和 MCP 验证。
- 本次更新不会增加或改变你的行情、账户和交易权限；实际可用功能仍以当前账户已经开通的权限为准。
