# 0.2.7 更新说明

这次更新在问财、交易兼容和安装升级修复的基础上，解决了不同安装包与本机插件版本显示不一致的问题。

## 本次修复

- 问财查询现在会返回证券名称以及问题中要求的数据列，结果更完整，也更容易阅读。
- 修复部分同花顺版本中交易功能因客户端内部字段变化而无法使用的问题。
- 删除已下线的订阅中心入口和所有会员提示，插件全部功能继续免费使用。
- 实时订阅只负责把同花顺数据返回给你，不再要求或捆绑 PostgreSQL。
- 修复 Level2 原生推送在并发订阅时可能混入其他证券记录的问题，逐笔委托、成交和撤单会先校验证券身份再返回。
- 实时委托卡片改为单只证券的挂单、撤单双列表，并在顶部持续显示当前价，避免把不同用途的数据挤在同一视图中。
- 明确 Tick 序列中的成交量、成交额为当日累计值；需要单笔成交明细时改用订单流成交查询。
- 安装和升级前会先检查目标文件，避免覆盖你自行放入的文件；升级时会自动清理旧版本遗留的受管文件。
- Codex、Claude Code、WorkBuddy、ZCode、OpenClaw 和 DeepSeek Harness 使用同一套同花顺本机能力。
- Codex、Claude Code、WorkBuddy、ZCode、OpenClaw 和 DeepSeek Harness 的安装包现统一为 0.2.7，升级时不会再混用旧版本文件。

## 安装

你可以直接让正在使用的 AI 助手安装本项目；自动安装失败时，再从本页附件下载对应安装包手动安装。升级完成后请重启同花顺；宿主没有显示新工具时，优先新建任务或重新加载插件。

## 兼容说明

- 已在同花顺 11.4.1.3 上完成行情、问财、实时订阅和交易状态验证。
- 针对同花顺 12.1.1.6 的交易兼容修复已加入本版本，仍欢迎使用该版本的用户反馈实际结果。
