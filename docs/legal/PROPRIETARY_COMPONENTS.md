# 闭源组件边界

本仓库公开八个 AI 客户端入口、公共安装配置、职责型技能、可选 Python SDK、文档和发行元数据。

FQGate 核心源码在独立私有仓库中维护，不进入本仓库。公开仓库的 `fqgate/` 目录只包含：

- FQGate 与 Agent 的版本兼容关系；
- 明确版本的公开下载地址；
- 文件名、大小和 SHA-256；
- 用户安装说明和实机验收状态。

FQGate 编译包只通过公开仓库 Release 分发，大型二进制不提交进 Git。构建脚本不得读取或复制同级私有仓库的源码、研究材料、调试符号、密钥或本机账户数据。
